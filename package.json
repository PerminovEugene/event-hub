{
  "name": "calendar",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.18.4",
    "typescript": "^3.7.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
