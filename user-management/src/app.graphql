type Cat {
  id: Int
  name: String
  age: Int
}

type Query {
  getCats: [Cat]
  cat(id: ID!): Cat
}

input CatInput {
  name: String
  age: Int
}

type Mutation {
  createCat(cat: CatInput): String
}
