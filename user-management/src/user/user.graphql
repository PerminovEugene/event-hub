type User {
  id: Int
  email: String
  role: String
}

type Query {
  login(email: String!, password: String!): User
}

input UserInput {
  email: String
  password: String
  passwordConfirm: String
}

type Mutation {
  registration(user: UserInput): User
}
