type SessionData {
  id: Int
  email: String
  role: String
  status: String
}

# type Query {
#   login(email: String!, password: String!): SessionData
# }

input RegistrationInput {
  email: String
  password: String
  passwordConfirm: String
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  registration(user: RegistrationInput): SessionData
}

type Mutation {
  login(email: String!, password: String!): SessionData
}

type Query {
  whoAmI: SessionData
}
