{
  "name": "gameserver",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-ts",
    "serve": "node dist/",
    "watch-node": "nodemon dist/",
    "dev": "npm run watch-ts && npm run serve",
    "test": "NODE_ENV=test jest --runInBand --forceExit --coverage --verbose --config ./jest.config.js",
    "build-ts": "tsc",
    "watch-ts": "tsc -w",
    "debug": "npm run build && npm run watch-debug"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.3",
    "ts-node": "^8.10.2",
    "tslint": "^5.11.0",
    "typescript": "^4.0.1"
  },
  "dependencies": {
    "@event-hub/game-shared": "^0.0.1",
    "@types/jest": "^26.0.5",
    "@types/restify": "^8.4.2",
    "@types/socket.io": "^2.1.11",
    "@types/supertest": "^2.0.10",
    "jest": "^26.1.0",
    "netcode": "^2.0.4",
    "restify": "^8.5.1",
    "socket.io": "^2.3.0",
    "supertest": "^4.0.2",
    "ts-jest": "^26.1.3"
  }
}
