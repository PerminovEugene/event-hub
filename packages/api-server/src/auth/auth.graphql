type SessionData {
  id: Int
  email: String
  role: String
  status: String
}

input RegistrationInput {
  email: String
  password: String
  passwordConfirm: String
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  registration(registrationInput: RegistrationInput): SessionData
  login(loginInput: LoginInput): SessionData

  # login($loginInput: LoginInput) {
  #   login(loginInput: $loginInput): SessionData
  # }
}

# type Mutation login($loginInput: LoginInput) {
#   hero(loginInput: $loginInput) {
#     name
#     friends {
#       name
#     }
#   }
# }

type Query {
  whoAmI: SessionData
}
