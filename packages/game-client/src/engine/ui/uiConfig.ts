export type UIConfig = {
    startX: number;
    startY: number;
    itemHeigth: number;
    itemWidth: number;
    dotRadius: number;
}

export const uiConfig: UIConfig = {
    startX: 10,
    startY: 10,
    itemHeigth: 30,
    itemWidth: 30,
    dotRadius: 6,
}